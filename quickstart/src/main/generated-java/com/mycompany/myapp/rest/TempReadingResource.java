/*
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Template pack-angular:src/main/java/rest/EntityResource.java.e.vm
 */
package com.mycompany.myapp.rest;

import static org.springframework.http.MediaType.APPLICATION_JSON_VALUE;
import static org.springframework.web.bind.annotation.RequestMethod.DELETE;
import static org.springframework.web.bind.annotation.RequestMethod.GET;
import static org.springframework.web.bind.annotation.RequestMethod.POST;
import static org.springframework.web.bind.annotation.RequestMethod.PUT;

import java.net.URI;
import java.net.URISyntaxException;
import java.util.List;
import java.util.Optional;

import javax.inject.Inject;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.bind.annotation.RequestBody;

import com.mycompany.myapp.domain.TempReading;
import com.mycompany.myapp.dto.TempReadingDTO;
import com.mycompany.myapp.dto.TempReadingDTOService;
import com.mycompany.myapp.dto.support.PageRequestByExample;
import com.mycompany.myapp.dto.support.PageResponse;
import com.mycompany.myapp.repository.TempReadingRepository;
import com.mycompany.myapp.rest.support.AutoCompleteQuery;

@RestController
@RequestMapping("/api/tempReadings")
public class TempReadingResource {

    private final Logger log = LoggerFactory.getLogger(TempReadingResource.class);

    @Inject
    private TempReadingRepository tempReadingRepository;
    @Inject
    private TempReadingDTOService tempReadingDTOService;

    /**
     * Create a new TempReading.
     */
    @RequestMapping(value = "/", method = POST, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<TempReadingDTO> create(@RequestBody TempReadingDTO tempReadingDTO) throws URISyntaxException {

        log.debug("Create TempReadingDTO : {}", tempReadingDTO);

        if (tempReadingDTO.isIdSet()) {
            return ResponseEntity.badRequest().header("Failure", "Cannot create TempReading with existing ID").body(null);
        }

        TempReadingDTO result = tempReadingDTOService.save(tempReadingDTO);

        return ResponseEntity.created(new URI("/api/tempReadings/" + result.id)).body(result);
    }

    /**
    * Find by id TempReading.
    */
    @RequestMapping(value = "/{id}", method = GET, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<TempReadingDTO> findById(@PathVariable Long id) throws URISyntaxException {

        log.debug("Find by id TempReading : {}", id);

        return Optional.ofNullable(tempReadingDTOService.findOne(id)).map(tempReadingDTO -> new ResponseEntity<>(tempReadingDTO, HttpStatus.OK))
                .orElse(new ResponseEntity<>(HttpStatus.NOT_FOUND));
    }

    /**
     * Update TempReading.
     */
    @RequestMapping(value = "/", method = PUT, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<TempReadingDTO> update(@RequestBody TempReadingDTO tempReadingDTO) throws URISyntaxException {

        log.debug("Update TempReadingDTO : {}", tempReadingDTO);

        if (!tempReadingDTO.isIdSet()) {
            return create(tempReadingDTO);
        }

        TempReadingDTO result = tempReadingDTOService.save(tempReadingDTO);

        return ResponseEntity.ok().body(result);
    }

    /**
     * Find a Page of TempReading using query by example.
     */
    @RequestMapping(value = "/page", method = POST, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<PageResponse<TempReadingDTO>> findAll(@RequestBody PageRequestByExample<TempReadingDTO> prbe) throws URISyntaxException {
        PageResponse<TempReadingDTO> pageResponse = tempReadingDTOService.findAll(prbe);
        return new ResponseEntity<>(pageResponse, new HttpHeaders(), HttpStatus.OK);
    }

    /**
    * Auto complete support.
    */
    @RequestMapping(value = "/complete", method = POST, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<List<TempReadingDTO>> complete(@RequestBody AutoCompleteQuery acq) throws URISyntaxException {

        List<TempReadingDTO> results = tempReadingDTOService.complete(acq.query, acq.maxResults);

        return new ResponseEntity<>(results, new HttpHeaders(), HttpStatus.OK);
    }

    /**
     * Delete by id TempReading.
     */
    @RequestMapping(value = "/{id}", method = DELETE, produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<Void> delete(@PathVariable Long id) throws URISyntaxException {

        log.debug("Delete by id TempReading : {}", id);

        try {
            tempReadingRepository.delete(id);
            return ResponseEntity.ok().build();
        } catch (Exception x) {
            // todo: dig exception, most likely org.hibernate.exception.ConstraintViolationException
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
    }
}