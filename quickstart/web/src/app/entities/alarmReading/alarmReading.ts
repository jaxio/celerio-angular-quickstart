//
// 
// Source code generated by Celerio, an Open Source code generator by Jaxio.
// Template pack-angular:web/src/app/entities/entity.ts.e.vm
//
import {Device} from '../device/device';
import {AlarmRule} from '../alarmRule/alarmRule';

export class AlarmReading {
    // Raw attributes
    id : number;
    dateRecorded : Date;
    // x-to-one
    deviceFk : Device;
    ruleFk : AlarmRule;


    constructor(json? : any) {
        if (json != null) {
            this.id = json.id;
            if (json.dateRecorded != null) {
                this.dateRecorded = new Date(json.dateRecorded);
            }

            if (json.deviceFk != null) {
                this.deviceFk = new Device(json.deviceFk);
            }

            if (json.ruleFk != null) {
                this.ruleFk = new AlarmRule(json.ruleFk);
            }
        }
    }

    // Utils

    static toArray(jsons : any[]) : AlarmReading[] {
        let alarmReadings : AlarmReading[] = [];
        if (jsons != null) {
            for (let json of jsons) {
                alarmReadings.push(new AlarmReading(json));
            }
        }
        return alarmReadings;
    }
}
